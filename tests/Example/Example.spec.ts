import { test, expect } from '@playwright/test';
import { exitCode } from 'process';

/*
test('Nombre del caso de uso', async ({page}) => {
  //  Ir a una pagina: await page.goto('login')
  
  //  Seleccionar por ID:  await page.locator("#username").fill(user);
  

  //  Clickear botón: await page.click('button[type="submit"]');

  //  Esperar a que el resultado sea una URL: await expect(page).toHaveURL('http://localhost:8080/siga-s3-2.0/principal');

  //  Esperar a que el resultado sea un elemento: await expect(page.getByRole('heading', { name: 'ANALISTA' })).toBeVisible();
});
*/